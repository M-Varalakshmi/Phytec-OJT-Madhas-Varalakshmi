#include <stdio.h>

int main() 
{
    float l,b;
    float Area;
    printf("enter the length and breadth\n");
    scanf("%f%f",&l,&b);
    Area = l*b;
    printf("area of rectangle is %f\n",Area);

    return 0;
}
