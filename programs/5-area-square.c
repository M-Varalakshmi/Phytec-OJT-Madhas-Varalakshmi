#include <stdio.h>

int main() 
{
    int Area,s;    
    printf("enter the length of side of square\n");
    scanf("%d",&s);
    Area = s*s;
    printf("area of square is %d\n",Area);

    return 0;
}
